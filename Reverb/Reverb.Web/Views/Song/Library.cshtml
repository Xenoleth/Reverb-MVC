@using Reverb.Web.Models.Song
@model ICollection<SongViewModel>

@Html.Partial("_SongSearchPartial", new SongSearchViewModel())

@if (Model.Count == 0)
{
    <p>No songs were found</p>
}

@foreach (var song in Model)
{
    @Html.Partial("_SongPartial", song);
}